apiVersion: v1
kind: Service
metadata:
  name: apisix-gateway
  namespace: ingress-apisix
spec:
  type: NodePort
  selector:
    app: apisix
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9080
